# Ztest D3/D7 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复流式身份 JSON vendor 泄漏，并提高强制工具调用生成完整必填参数的稳定性。

**Architecture:** 身份修复沿用现有跨 chunk 精确短语过滤；工具修复在 Anthropic→Kiro 转换阶段从规范化 schema 生成 Required-only 描述提示，不改响应参数。

**Tech Stack:** Rust、serde_json、Cargo 单元测试、Docker 服务器增量构建

---

### Task 1: 身份 JSON vendor 归一化

**Files:**
- Modify: `src/anthropic/identity.rs`

- [ ] 添加失败测试，断言紧凑和带空格 vendor 字段均变为 Anthropic，且跨 chunk 任意切分一致。
- [ ] 用 `rustc --test src/anthropic/identity.rs` 运行并确认测试因 Amazon Web Services 未被改写而失败。
- [ ] 在精确身份短语表加入两种 vendor 字段形式。
- [ ] 重新运行单文件测试并确认通过。

### Task 2: Required 工具 schema 提示

**Files:**
- Modify: `src/anthropic/converter.rs`

- [ ] 添加失败测试，构造 required 字段、`const` 和单值 `enum` schema，断言 RequiredSpecific 描述包含字段和值且 Auto 不注入。
- [ ] 在服务器运行聚焦 converter 测试并确认缺少提示导致失败。
- [ ] 实现 `required_tool_schema_nudge`，只读取 required、const 和单值 enum，生成短提示。
- [ ] 在 RequiredAny/RequiredSpecific 分支追加提示；required 为空时不追加空对象警告。
- [ ] 重新运行聚焦测试并确认通过。

### Task 3: 集成、部署与真实 API 验证

**Files:**
- Modify: `docs/superpowers/plans/2026-07-13-ztest-d3-d7.md`（勾选完成项）

- [ ] 运行 `cargo fmt --check`、聚焦测试和服务器 release 构建。
- [ ] 提交修复并合并回本地 master。
- [ ] 将指定 commit 部署到公开 8991 测试容器，不修改 8990。
- [ ] 连续验证身份 JSON vendor 为 Anthropic，以及 get_weather 参数包含 city/nonce。

