server {
    listen 80;
    server_name rs-test.TEST-HOST-IP-DASHED.sslip.io;

    access_log /www/wwwlogs/rs-test.TEST-HOST-IP-DASHED.sslip.io.log combined;
    error_log /www/wwwlogs/rs-test.TEST-HOST-IP-DASHED.sslip.io.error.log;

    location ^~ /.well-known/acme-challenge/ {
        root /www/wwwroot/rs-test.TEST-HOST-IP-DASHED.sslip.io;
        allow all;
    }

    location / {
        return 404;
    }
}
