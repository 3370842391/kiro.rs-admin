# 企业号有界重试与升级配置

企业号专项支持四个候选端点、有效首事件预读、固定发送预算和账号共享节流。普通账号继续使用自己的路由与重试规则。不同端点不代表拥有独立上游配额。

## 升级时核对的参数

将下面字段合并到现有配置，保留其它设置和凭据：

```json
{
  "enterpriseSpecialHandling": true,
  "enterpriseDefaultEndpoint": "ide",
  "enterpriseMaxRetries": 4,
  "maxBucketAttemptsPerRequest": 3,
  "enterpriseRetry": {
    "endpoints": ["ide", "runtime", "amazonq", "codewhisperer"],
    "firstEventTimeoutMs": 10000,
    "totalTimeoutMs": 30000
  },
  "streamIdleTimeoutSecs": 120
}
```

- `enterpriseMaxRetries` 现在是**包含首次的最大真实企业发送次数**，不再是可以不断续期的外层重试轮数。
- 当 `maxBucketAttemptsPerRequest > 0` 时，实际次数还受它加1的限制；上例最多4次。该值为0只解除这一项限制，企业次数和时间预算仍生效。
- `firstEventTimeoutMs` 从真正发送开始约束响应头与有效内容的等待。纯协议字节、半帧及一般元数据不算首事件成功。
- `totalTimeoutMs` 约束准备、选号和启动重试，包含token/profile/模型准备。预留 `min(totalTimeoutMs / 3, 5000)` 毫秒给个人号兜底；总预算30秒时企业阶段最多约25秒。已开始正常输出的回答可以持续更久，中途无上游数据受 `streamIdleTimeoutSecs` 约束。
- `enterpriseDefaultEndpoint` 是启用列表的轮询原点；首发端点还会随请求轮换，不保证每次从该端点开始。
- 参数对入站请求取快照。热更新影响新请求，不会立即取消已经开始的请求。

## 账号并发和发送节奏

在账号设置中配置非零 `maxConcurrency`。独立测试号可从1开始分级验证容量；已有生产负载应根据完整请求耗时和当前占用小步调整，不能把单号短请求测试值直接套到线上。0保持不限并发的原语义。选号和并发预约原子执行，许可持续到响应完整消费、错误或取消时才释放，因此持续数分钟的正常回答也会占用一个位置。

企业API、MCP及切换端点共享该账号的发送节奏。`rpmLimit > 0` 时，主动发起的真实发送至少间隔 `ceil(60000 / rpmLimit)` 毫秒，例如100对应600毫秒；0不设置基础RPM间隔。面板原有业务RPM仍按逻辑调用统计，真实上游发送量见请求trace和发送日志。

普通429会触发同账号共享的短指数退避，有效 `Retry-After` 优先遵守。状态只在当前进程内共享；同一账号被多个实例使用时，应协调各实例的总负载。

企业阶段结束后不会重新获得一轮企业预算。若请求分组有可用个人号，可在剩余预算内兜底；没有可用兜底时返回错误。已确认普通429对外返回 `429/rate_limit_error`，已握手的SSE通过错误事件传递。账号额度、鉴权、未知错误体和其它失败保持各自分类。

## 线上调参时区分的现象

- `no_available_credentials` / 503：当前请求没有可调度账号。检查同一分组和模型范围内的启用账号、在途并发、RPM、冷却及本次请求已排除的账号；不等于所有账号已禁用。提高发送重试次数无法补足并发容量。
- `enterprise_first_event_timeout`：该跳在截止前没有提供有效事件，即使HTTP状态为200也可能发生。失败详情包含“前一跳超时、后一跳成功”的请求，应同时查看最终结果。
- `streamIdleTimeoutSecs`：限制连续没有上游数据的时间，不限制持续输出的总时长。热更新后新响应使用新值，已开始的流不会重新读取该阈值。

先单独调整并发并观察，再决定是否放宽首事件和总启动预算。可按业务需要试验20秒首事件、60秒总启动预算，但等待更久会占用更多并发；不要同时解除发送次数上限。各轮至少覆盖正常请求完成所需的时间，对比完整完成量、本地拒绝、上游429、首事件超时与账号状态；成功样本的首字速度不能代表全部请求的体验。保留变更前的字段值，回滚只恢复本轮参数，避免覆盖刷新后的凭据和用量。

## 验证与部署

先在测试环境确认正常正文、SSE结束事件、真实命中账号及端点、实际发送次数和请求结束后的并发归零，再逐步增加负载。成功HTTP状态本身不能代替完整响应验证。

本地假上游回归涵盖持续429、共享等待、慢响应头和错误体、半帧、API/MCP共享预算、取消释放与并发竞争。真实端点速度和可用容量仍需使用独立测试流量验证。

部署前保存当前镜像和配置。回滚时保留运行期间更新的OAuth凭据与用量数据，避免恢复过期token或覆盖新用量。仓库的 `Dockerfile` 可从源码构建后端和管理界面；仅提交代码不会自动生成发布镜像。
